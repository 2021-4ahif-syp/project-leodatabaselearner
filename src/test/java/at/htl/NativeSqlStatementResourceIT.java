package at.htl;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeSqlStatementResourceIT extends SqlStatementResourceTest {

    // Execute the same tests but in native mode.
}